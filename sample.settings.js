exports.SETTINGS = {
    BASE_FILENAME: 'base.vs',
    TARGET_BITRATE: 9000,
    TOLERANCE: 500,
    BLACK_THRESHOLD: 30,
    ROW_TO_ROW_THRESHOLD: 5 //threshold between average RGB values of two adjacent rows (average across all pixels in that row)
}